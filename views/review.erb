<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="/style.css?<%=Time.now.to_i%>">
</head>
<body>
    <h2><%=@movieinfo['name']%></h2>
    <p>Release year: <%=@movieinfo['year']%> </p>
    <div class="imdb" style="display: flex; height: 18px; flex-direction: row;">
        <img src="/img/imdblogo.png" style="">
        <p style="margin: 0;"> : <%=@movieinfo['imdb_rating']%> </p>
    </div>
    <p>Runtime: <%=@movieinfo['runtime']%> min </p>
    <p>Genre<%=@movieinfo['genres'].split(',').length > 1 ? "s": ""%>: <%= @movieinfo['genres'].split(",").join(", ")%></p>
    <p>Streaming service<%=(@movieinfo['services'].split(",").length > 1 ? "s": "")%>: <%=@movieinfo['services'].split(",").join(", ")%></p>
    <img src="<%=@movieinfo['poster']%>" style="width: 300px;
                                                height:450px; 
                                                position: absolute; 
                                                right: 5%; 
                                                top: 10.9%;
                                                border-radius: 25px;">
    <form action="/review/<%=@movieinfo['id']%>" method="POST" style="
                                                                    display: flex;
                                                                    flex-direction: column;
                                                                    width: 300px;
                                                                    gap: 20px;">
        <h3>Your review</h3>
        <input type="text" name="score" placeholder="Your rating /10" style="color: black"></input>
        <textarea class = "review" type="text" name="note" placeholder="Your review" style="color:black; text-wrap:wrap;"></textarea>
        <input type="submit" value="Finish" style="color:black; width:50px;border-radius:10px;">
    </form>
</body>
</html>